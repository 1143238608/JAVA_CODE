// 使用 fetch API 从后端获取数据
// const page = 1;
// fetch(`/lck1/${page}`)  // 假设你的后端接口地址是 '/api/data'
//     .then(response => response.json())  // 将响应数据解析为 JSON
//     .then(data => {
//         const myDiv = document.getElementById('lckInfo');
//
//         // 如果后端返回的是一个对象或数组，你可以将其转换为字符串并显示
//         // myDiv.innerHTML = JSON.stringify(data, null, 2);
//
//     })
//     .catch(error => {
//         console.error('获取数据出错:', error);
//     });


